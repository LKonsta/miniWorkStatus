import Settings from './Settings';

const Header: React.FC = () => {
    return (
        <div>
            <h1>Mini Status</h1>
            <Settings />
        </div>
    );
};

export default Header;